package nn.net_Math;

public interface Activation
{
  /**
   * This functional interface is used to return
   * the result of the activation method using the
   * function.
   */
  double normalize(double x);
}
